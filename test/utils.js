export const getByTag = (wrapper, tag) => wrapper.find(tag);
export const getByClassName = (wrapper, className) => wrapper.find(className);
export const getById = (wrapper, id) => wrapper.find(id);
export const getBySelector = (wrapper, selector) => wrapper.find(selector);
